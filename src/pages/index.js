import Layout from '../components/Layout';
import HomePageContent from '../components/HomePageContent';

const Home = () => {
  return (
    <Layout>
      <HomePageContent />
    </Layout>
  );
};

export default Home;
